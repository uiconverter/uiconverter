import { useState } from 'react';
import { useRef } from 'react';

export const page = () => {
  const [state, setState] = useState();
  const ref = useRef();

  return (
    <>
      <>hi</>
    </>
  );
};
